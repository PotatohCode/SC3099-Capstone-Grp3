# NOTE: several pins below were bumped from the original skeleton versions
# because they predate Python 3.13 wheel builds (psycopg2-binary, pydantic /
# pydantic-core, cryptography, bcrypt) — the old pins force a from-source
# build that needs pg_config / a Rust toolchain / MSVC linker most dev
# machines won't have. If your machine is on Python 3.11/3.12 these would
# have worked as-is too, so this bump is safe either way.
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.36  # 2.0.25 breaks on Python 3.13's typing internals
alembic==1.13.3
psycopg2-binary==2.9.12  # 2.9.9 has no Python 3.13 wheel
pydantic==2.9.2  # 2.5.3's pydantic-core has no Python 3.13 wheel
pydantic-settings==2.6.1
pydantic[email]
email-validator
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
redis==5.0.1
httpx==0.26.0
python-dotenv==1.0.0

# Security
cryptography==44.0.1  # 42.0.0 has no Python 3.13 wheel
bcrypt==4.2.1  # 4.1.2 has no Python 3.13 wheel

# Observability
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-instrumentation-sqlalchemy==0.43b0
opentelemetry-exporter-otlp==1.22.0
prometheus-client==0.19.0

# Geolocation
geopy==2.4.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
faker==22.0.0
