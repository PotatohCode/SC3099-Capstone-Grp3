fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6
redis==5.0.1
httpx==0.26.0

# Face recognition & CV
# MediaPipe: face detection (detect_face) and liveness depth analysis
# (analyze_face_mesh) only - NOT identity matching. Landmark-based identity
# embeddings were tried first and empirically failed to tell real people
# apart (see git history) - that's what face-recognition/dlib is for below.
mediapipe==0.10.18  # pinned - newer versions removed mp.solutions
opencv-python>=4.9.0.80
numpy>=1.24.3
pillow>=10.2.0

# face-recognition: the actual identity-matching model
# (extract_face_embedding/generate_face_hash). Builds dlib from source on
# install (no prebuilt wheel for every platform/Python combo) - verified
# working on Python 3.12/Windows.
face-recognition==1.3.0
# Required alongside face-recognition: face-recognition-models (its
# dependency) still uses the pkg_resources API, which newer setuptools
# versions have removed (deprecated 2025, slated for full removal).
# Without this pin, installation succeeds but face_recognition fails at
# import time with "ModuleNotFoundError: No module named 'pkg_resources'".
setuptools<81

# Observability
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-exporter-otlp==1.22.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
